#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n,i,j,a,b,sum,s=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%d",&a);
        for(j=1;j<=a;j++)
        {
        	scanf("%d",&b);
        	sum=s+1;
				
		}
	if(sum>=9)
	{printf("Case %d: All hail to princess mAina\n",i);}
	else
	{printf("Case %d: :'(\n",i);}
	}
	
	return 0;
}
