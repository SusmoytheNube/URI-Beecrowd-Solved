#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
	int a,b,c,d,area;
	scanf("%d%d",&a,&b);
	scanf("%d%d",&c,&d);
	area = (a*b)+(c*d);
	printf("%d",area);    
	return 0;
}
